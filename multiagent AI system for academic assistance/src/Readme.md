# Application Source Code Repository

## System Directory Mappings
* **`backend/`**: Contains the core Node.js server configurations (`server.js`), multi-agent route handlers (`routes/`), data token checking scripts (`utils/`), and structural Gemini API code wrappers (`agents/`).
* **`frontend/`**: The complete Vite-React UI layout dashboard code processing customer chat panels.
